#!/usr/bin/env bash

set -eu

readonly ROOT_DIR="$(realpath "$(dirname "$(realpath "$0")")/../..")"

readonly PACKAGE_TO_SYNCHRONIZE="core-contract"
source "${ROOT_DIR}"/bin/synchronize/synchronize.inc.bash
